{
  "name": "spryntax",
  "version": "1.0.0",
  "description": "",
  "main": "spryntax.js",
  "scripts": {
    "start": "npm install && node ./node_modules/pm2/bin/pm2 start spryntax.js",
    "stop": "node ./node_modules/pm2/bin/pm2 stop spryntax.js",
    "restart": "npm stop && npm start",
    "kill": "node ./node_modules/pm2/bin/pm2 kill",
    "status": "node ./node_modules/pm2/bin/pm2 status",
    "desc": "# start: Installs and/or updates all dependencies and starts the Node server for spryntax.js\n# stop: Stops the Node server for spryntax.js\n# restart: Restarts the Node server for spryntax.js\n# kill: Stops PM2 (including the Node server for spryntax.js\n# status: Gets the status of current PM2 applications"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "express-handlebars": "^8.0.1",
    "pm2": "^5.4.3"
  }
}
